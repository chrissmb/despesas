<template>
  <button
    @click="$emit('click')"
    :class="'btn ' + type"
    :disabled="disabled"
    :type="notSubmit ? 'button' : 'submit'"
  >
    <slot></slot>
  </button>
</template>

<script>
export default {
  name: "btn",
  props: {
    type: String,
    disabled: {
      type: Boolean,
      default: false,
    },
    notSubmit: {
      type: Boolean,
      default: false,
    },
  },
};
</script>

<style scoped>
.btn {
  margin: 5px;
  padding: 8px 10px;
  border-radius: 4px;
  border: solid 1px #ccc;
  text-decoration: none;
  font-size: 16px;
  display: inline-block;
}

.btn:hover:enabled:not(.disabled) {
  background: linear-gradient(#ddd, #ccc);
}

.btn.primary {
  background-color: #36f;
  color: white;
  font-weight: 100;
  border-color: #36f;
}

.btn.primary:hover:enabled:not(.disabled) {
  background: linear-gradient(#36f, #36b);
}

.btn.success {
  background-color: #0a0;
  color: white;
  font-weight: 100;
  border-color: #0a0;
}

.btn.success:hover:enabled:not(.disabled) {
  background: linear-gradient(#0a0, #080);
}

.btn.error {
  background-color: #c00;
  color: white;
  font-weight: 100;
  border-color: #c00;
}

.btn.error:hover:enabled:not(.disabled) {
  background: linear-gradient(#c00, #800);
}

.btn.warning {
  background-color: #d60;
  color: white;
  font-weight: 100;
  border-color: #d60;
}

.btn.warning:hover:enabled:not(.disabled) {
  background: linear-gradient(#d60, #c30);
}

.btn.secondary {
  background-color: #6af;
  color: white;
  font-weight: 100;
  border-color: #6af;
}

.btn.secondary:hover:enabled:not(.disabled) {
  background: linear-gradient(#6af, #37e);
}

.btn:disabled {
  opacity: 0.4;
}

.btn.disabled {
  opacity: 0.4;
  pointer-events: none;
}
</style>
